afterEach(async () => {
  // await TestDataSource.initialize();
  // const container = getDefaultContainer(TestDataSource);
  // // eslint-disable-next-line @typescript-eslint/no-explicit-any
  // const keys = Array.from((container as any)._factories.keys()) as string[];
  // keys.forEach((key: string) => {
  //   const instance = container.constitute(key) as {
  //     onShutdown?: () => void;
  //   };
  //   if (instance?.onShutdown) {
  //     instance.onShutdown();
  //   }
  // });
  // await TestDataSource.destroy();
});
